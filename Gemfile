# frozen_string_literal: true
source "https://rubygems.org"

# Add the Jekyll gem itself
gem "jekyll", "~> 4.2"  # Or use a version compatible with the theme if known

# Add Jekyll plugins based on _config.yml or theme requirements.
# Look inside _config.yml for lines like:
# plugins:
#   - jekyll-feed
#   - jekyll-seo-tag
#
# Or for lines like:
# theme: minima
# remote_theme: owner/repo
#
# If you see plugins listed, add them here like:
# gem "jekyll-feed"
# gem "jekyll-seo-tag"
#
# If you see a 'theme:', add it like:
# gem "minima"
#
# If you see 'remote_theme:', you might need:
# gem "jekyll-remote-theme"

# If using GitHub Pages, uncomment this:
# gem "github-pages", group: :jekyll_plugins

# Since you have a _sass folder, Jekyll's built-in converter
# should work, but sometimes themes require a specific one.
# gem "jekyll-sass-converter" # Usually not needed unless specified